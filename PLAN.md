# File Transfer 实施计划

## 1. 实施原则

- 先冻结文件格式和测试向量，再开发界面，避免编解码兼容性被 UI 反向影响。
- Python 版与 Node.js 版是两套互不依赖的完整实现，共同遵循同一格式规范和测试向量。
- 两个版本的功能、界面、CLI 行为、错误语义和编码结果必须兼容。
- 每个阶段都形成可运行、可验证的增量。
- 两版均采用本地网页界面，不引入 Electron；双击启动后自动打开默认浏览器。
- 只提供源码运行模式和 `.command`/`.bat` 启动器，不制作 `.exe` 或 `.app`。

## 2. 阶段计划

### 阶段 0：需求确认与技术基线

目标：冻结 MVP 范围和运行环境。

任务：

1. 确认默认最大文件大小，暂定 100 MiB。
2. 确认 Python 版采用 Python 3.11+，Node.js 版采用 Node.js 20 LTS+；用户只需安装所选版本的运行时。
3. 确认首版不兼容参考项目的旧 `0xAF` 图片格式。
4. 确认发布形态为“两套源码版本 + 各自的双击启动脚本”，不提供独立安装包。
5. 建立 README、许可证说明、忽略文件和基础目录。

交付物：确定版需求文档、项目骨架、运行环境说明。

完成条件：待确认项有明确决策，开发者可按文档初始化环境。

### 阶段 1：设计并冻结 v1 文件格式

目标：得到稳定、可验证、可扩展的二进制格式。

任务：

1. 定义魔数、版本、字段字节序、头部长度、64 位文件长度、UTF-8 文件名和 SHA-256 布局。
2. 明确空文件、超长文件名、非法 UTF-8、未知字段和未知版本的行为。
3. 定义像素映射、末尾填充和正方形边长计算规则。
4. 制作固定测试向量：空文件、1–4 字节、中文文件名和小型二进制文件。
5. 编写格式规范文档，并记录 v1 兼容承诺。

交付物：格式规范、测试向量及预期哈希/图片尺寸。

完成条件：仅根据规范即可独立实现兼容编码器或解码器；所有边界行为无歧义。

### 阶段 2：实现 Python 完整版本

目标：形成不依赖 Node.js 的 Python 完整版本。

任务：

1. 实现元数据序列化、反序列化和安全文件名处理。
2. 实现文件到 PNG 的编码。
3. 实现 PNG 检查、解码和 SHA-256 校验。
4. 实现 `encode`、`decode`、`inspect` CLI 及 `--json` 输出。
5. 实现仅监听 `127.0.0.1` 的 Python 本地服务和共享浏览器 UI。
6. 设计稳定错误类型与退出码。
7. 增加输入大小、图片尺寸、PNG 解压上限和内存安全检查。
8. 添加单元测试、API 测试、边界测试和端到端往返测试。

交付物：可独立使用的 Python codec、CLI、本地服务与浏览器 UI。

完成条件：所有测试向量通过；随机文件往返哈希一致；损坏输入能够被识别。

### 阶段 3：实现 Node.js 完整版本

目标：按照同一规范形成不依赖 Python 的 Node.js 完整版本。

任务：

1. 根据 `FORMAT.md` 和固定测试向量独立实现 Node.js codec。
2. 实现 `encode`、`decode`、`inspect` CLI，并与 Python 版保持参数、JSON 和退出码一致。
3. 建立只监听 `127.0.0.1` 的 HTTP 服务，复用同款浏览器 UI。
4. 实现临时上传、任务 ID、状态查询和结果下载。
5. 实现输入大小限制、并发限制、超时和取消后的清理。
6. 实现随机短生命周期下载标识，禁止直接传入服务器文件路径。
7. 实现端口自动选择、已有实例识别和优雅退出。
8. 添加单元测试、API 测试、边界测试和端到端往返测试。

交付物：可独立使用的 Node.js codec、CLI、本地服务与浏览器 UI。

完成条件：通过 API 可完成一次编码、解码和检查；异常任务不会导致服务退出或泄漏临时文件。

### 阶段 4：统一界面与跨版本互操作

目标：确保两个版本对用户呈现相同体验，并能互相解码。

任务：

1. 固化共享页面的编码/解码入口、拖放、文件信息、进度、下载和重试交互。
2. 统一两版 API、CLI JSON、错误代码、错误文案和预计尺寸算法。
3. 用 Python 编码并由 Node.js 解码全部固定测试向量。
4. 用 Node.js 编码并由 Python 解码全部固定测试向量。
5. 比较两个版本解析出的文件名、大小、哈希、格式版本和图片尺寸。
6. 增加隐私说明和“图片不可被缩放或有损转码”的醒目提醒。
7. 进行键盘操作、基础无障碍和主要浏览器测试。

交付物：两版一致的浏览器 UI、共享行为契约与跨版本互操作测试。

完成条件：用户无需命令行即可完成验收用例中的往返转换。

### 阶段 5：跨平台安装与双击启动

目标：在 macOS 和 Windows 上提供低门槛启动体验。

任务：

1. 为 Python 版编写 `start.command` 和 `start.bat`，检查 Python 3.11+ 并启动服务。
2. 为 Node.js 版编写 `start.command` 和 `start.bat`，检查 Node.js 20+ 并启动服务。
3. 确保每个启动器只检查本版本运行时，不要求另一种运行时存在。
4. 所有脚本正确处理项目路径、中文和空格。
5. 启动服务后自动打开默认浏览器；失败时保留窗口显示日志。
6. 验证重复启动、端口占用、Ctrl+C/关闭窗口和异常退出清理。
7. 编写 macOS 执行权限/Gatekeeper 相关说明。

交付物：两个版本共四个跨平台启动脚本，以及启动和排障文档。

完成条件：两端干净测试环境均能按照 README 从安装到完成首次转换。

### 阶段 6：质量验证与发布准备

目标：完成 MVP 验收并形成可发布版本。

任务：

1. 执行空文件、边界字节数、Unicode、随机二进制、大文件和损坏 PNG 测试。
2. 在 Windows 10/11、macOS Apple Silicon 上分别验收两个版本；条件允许时补充 Intel macOS。
3. 记录 1/10/50/100 MiB 文件的耗时、峰值内存和 PNG 体积。
4. 根据基准测试确定默认文件上限。
5. 检查本地绑定、路径穿越、命令注入、临时文件和资源耗尽风险。
6. 完善 README、故障排查、格式兼容承诺和已知限制。
7. 执行双向跨版本互操作测试，确认两版生成物兼容。
8. 打版本标签并保留测试向量，作为未来版本回归基线。

交付物：测试报告、性能基线、发布说明和 MVP 版本。

完成条件：`REQUIREMENTS.md` 中 MVP 验收标准全部通过，或例外项有明确记录与批准。

## 3. 推荐开发顺序

```text
需求决策
  → v1 格式规范与测试向量
  → Python 完整版本
  → Node.js 完整版本
  → 统一 UI 与双向互操作
  → 两版 macOS/Windows 启动器
  → 跨平台验收与发布
```

格式规范与测试向量冻结后，两套实现可以并行推进；任何一版不得以另一版的运行时代码作为依赖。

## 4. 测试策略

### 4.1 两版 codec 单元测试

- 头部序列化/反序列化。
- 64 位长度和字节序。
- UTF-8 文件名及安全清理。
- 图片边长和填充计算。
- 魔数、版本、容量和哈希错误。

### 4.2 往返与固定向量测试

- 空文件及 1、2、3、4 字节文件。
- 可压缩文本、不可压缩随机数据、ZIP、已有 PNG。
- 空格、中文、emoji、多点扩展名和长文件名。
- 固定输入对应固定逻辑载荷；PNG 编码器元信息差异不得影响解码结果。

### 4.3 两版 API 测试

- 上传限制、任务状态、下载一次性标识和过期行为。
- 编解码成功、结构化错误、超时和任务异常退出。
- 并发请求、取消和临时目录清理。
- 只监听回环地址。

### 4.4 跨版本互操作测试

- Python 编码、Node.js 检查并解码。
- Node.js 编码、Python 检查并解码。
- 两版对固定测试向量返回一致的元数据、错误类型和退出码。
- 两版格式主版本、头部字段、字节序、补零和哈希行为一致。

### 4.5 跨平台验收

- 从含中文及空格的路径双击安装和启动脚本。
- 缺少运行时、依赖未安装、端口被占用和重复启动。
- Finder/资源管理器选择文件并完成真实往返。
- 服务退出及异常退出后的临时文件处理。

## 5. 主要风险与缓解措施

| 风险 | 影响 | 缓解措施 |
| --- | --- | --- |
| 大文件导致高内存 | 进程被杀或系统卡顿 | 提前估算、默认上限、分块处理、性能基准 |
| PNG 被平台转码 | 无法恢复或哈希失败 | UI 明示限制、严格 SHA-256 校验 |
| 运行时缺失或版本过低 | 双击无法启动 | 各版本独立检查并显示明确安装指引 |
| 两套实现行为漂移 | 跨版本图片无法恢复 | 共同规范、固定测试向量、双向互操作测试 |
| 文件名或路径注入 | 覆盖文件或执行命令 | 基础文件名清理、数组参数调用、受控临时目录 |
| 格式过早变更 | 已生成图片无法兼容 | 先冻结规范和测试向量、主次版本策略 |
| macOS 脚本执行权限 | Finder 双击失败 | 发布流程保留执行位、README 提供修复命令、真机验证 |
| 源码分发需要一种运行时 | 初次使用仍有门槛 | 清晰安装指引；两版均保持零第三方依赖 |

## 6. MVP 之后的候选迭代

1. 批量文件自动打包与还原。
2. 可选压缩，以降低可压缩数据生成的图片尺寸。
3. 可选密码加密与认证加密。
4. Legacy `0xAF` 格式只读迁移。
5. 文件分片为多张 PNG，并生成清单与顺序校验。
6. 拖文件到启动器图标或脚本上直接执行转换。

## 7. 开工前决策清单

- [ ] 默认最大文件大小是否采用 100 MiB。
- [ ] 是否明确不支持 Intel macOS，或将其纳入首版测试。
- [ ] 是否需要读取参考 TypeScript `0xAF` 格式生成的旧图片。
- [x] 提供两个独立版本，用户只需预装 Python 或 Node.js 中的一种。
- [x] 不制作 `.exe`、`.app` 或 Electron 安装包。
- [ ] 首版 UI 是否只需要中文。
- [ ] 默认输出是否允许覆盖现有文件；建议不覆盖并自动添加序号。
