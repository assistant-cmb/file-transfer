# File Transfer 项目需求文档

## 1. 文档信息

- 项目名称：File Transfer
- 文档版本：v0.1
- 当前阶段：需求与方案设计
- 目标平台：macOS、Windows
- 技术约束：分别提供纯 Python 版与纯 Node.js 版；两个版本互不依赖
- 参考实现：`/Users/conrad/Desktop/workspace/toolkit/utils/encode-file`

## 2. 背景

现有参考工具可以把任意文件的二进制数据映射到 RGB 像素并保存为无损 PNG，也可以从 PNG 还原文件。但现有使用方式存在以下问题：

- Python 旧版解码时需要手工提供原文件大小，容易出错。
- TypeScript 版只记录 24 位文件长度，单文件理论上限约为 16 MiB。
- 编码结果未完整保存原文件名、扩展名和校验值。
- 缺少适合普通用户的独立、跨平台启动方式。
- 缺少统一的 CLI、错误提示、进度反馈和自动化测试。

本项目将其整理为两个功能一致、可独立运行的版本。用户根据本机已有环境选择 Python 版或 Node.js 版即可，既保留命令行能力，也提供双击脚本即可打开的本地网页界面。

## 3. 产品目标

### 3.1 核心目标

1. 用户可以选择或拖入任意文件，将其转换为 PNG 图片。
2. 用户可以选择由本工具生成的 PNG，自动还原原始文件名和内容。
3. macOS 用户双击 `.command`、Windows 用户双击 `.bat` 即可启动本地界面。
4. Python 版只依赖 Python 生态，Node.js 版只依赖 Node.js 生态，不要求同时安装两种运行时。
5. 编码结果具备版本识别、文件元数据和 SHA-256 完整性校验能力。
6. 所有文件只在本机处理，不上传到任何远程服务器。
7. 两个版本遵循同一格式规范，生成的 PNG 必须能够相互解码。

### 3.2 成功标准

- 对受支持大小范围内的任意二进制文件执行“文件 → PNG → 文件”后，SHA-256 完全一致。
- 在干净的 Windows 10/11 与受支持 macOS 环境中，可通过双击启动脚本进入界面。
- 用户不需要输入原文件大小、文件名或扩展名。
- 非本工具图片、损坏图片、版本不兼容和依赖缺失时，均显示可理解且可操作的错误提示。

## 4. 用户与典型场景

### 4.1 目标用户

- 希望以 PNG 形式临时保存或传递小型文件的普通用户。
- 需要批处理或自动化转换的开发者。
- 需要完全离线处理文件的用户。

### 4.2 核心流程

#### 文件转图片

1. 用户双击平台启动脚本。
2. 默认浏览器打开本地页面。
3. 用户拖入或选择一个文件。
4. 页面展示文件名、大小和预计图片尺寸。
5. 用户点击“转换为图片”。
6. 转换完成后，用户下载或保存 PNG。

#### 图片转文件

1. 用户切换到“图片转文件”。
2. 用户拖入或选择 PNG。
3. 工具校验格式、版本、数据长度和 SHA-256。
4. 页面展示即将恢复的文件名和大小。
5. 用户点击“还原文件”并保存结果。

#### 命令行处理

开发者可调用任一版本的 CLI 编码或解码，获得明确的退出码、输出路径和机器可读结果。

## 5. 功能需求

### 5.1 编码

- FR-ENC-001：支持选择或拖放任意单个文件。
- FR-ENC-002：将文件数据无损映射为 RGB PNG；不得输出 JPEG、WebP 等可能有损的格式。
- FR-ENC-003：自动保存原文件名、原始字节数、SHA-256、格式版本及必要的编码参数。
- FR-ENC-004：默认输出名为 `<原文件名>.png`，重名时由浏览器或 CLI 输出规则安全处理。
- FR-ENC-005：转换前显示原文件大小和预计图片分辨率。
- FR-ENC-006：转换期间显示阶段或进度，完成后显示 PNG 大小与分辨率。
- FR-ENC-007：首版支持单文件；批量处理作为后续增强项。

### 5.2 解码

- FR-DEC-001：只接受 PNG 输入，并校验工具魔数和格式版本。
- FR-DEC-002：从图片内部读取原始文件名和长度，不要求用户手工填写。
- FR-DEC-003：输出文件名必须经过路径清理，只保留安全的基础文件名，禁止目录穿越。
- FR-DEC-004：恢复后计算 SHA-256；校验失败时不得将结果标记为成功。
- FR-DEC-005：对非本工具图片、数据容量不足、元数据损坏、校验失败、版本不支持分别给出明确错误。
- FR-DEC-006：解码前可展示原文件名、大小和格式版本。

### 5.3 本地网页界面

- FR-UI-001：提供“文件转图片”和“图片转文件”两个清晰入口。
- FR-UI-002：支持点击选择与拖放。
- FR-UI-003：显示待处理文件信息、运行状态、结果和错误信息。
- FR-UI-004：提供结果下载按钮；任务结束后及时清理服务端临时文件。
- FR-UI-005：页面刷新或服务重启后，不保留用户文件内容。
- FR-UI-006：界面默认使用中文，并为后续国际化保留结构。

### 5.4 双击启动与退出

- FR-LAUNCH-001：Python 版和 Node.js 版分别提供自己的 `start.command`，可从 macOS Finder 双击运行。
- FR-LAUNCH-002：Python 版和 Node.js 版分别提供自己的 `start.bat`，可从 Windows 资源管理器双击运行。
- FR-LAUNCH-003：每个启动器只检查本版本所需的运行时和依赖；缺失时展示安装指引并暂停窗口，避免错误一闪而过。
- FR-LAUNCH-004：启动成功后自动打开默认浏览器，访问仅绑定本机回环地址的页面。
- FR-LAUNCH-005：避免重复启动；端口占用时优先识别已有实例，否则自动选择可用端口。
- FR-LAUNCH-006：终端窗口中显示访问地址和退出方法；用户退出服务后完成临时文件清理。
- FR-LAUNCH-007：两个版本均不得在启动时联网安装依赖；运行时缺失或版本过低时应显示明确安装指引。

### 5.5 CLI

- FR-CLI-001：Python CLI 至少支持：
  - `python -m file_transfer encode <input> [-o output.png]`
  - `python -m file_transfer decode <input.png> [-o output-or-directory]`
  - `python -m file_transfer inspect <input.png> [--json]`
- FR-CLI-002：Node.js CLI 至少支持：
  - `node src/cli.js encode <input> [-o output.png]`
  - `node src/cli.js decode <input.png> [-o output-or-directory]`
  - `node src/cli.js inspect <input.png> [--json]`
- FR-CLI-003：两个版本成功退出码均为 `0`；参数、输入、格式、校验和内部错误使用一致的稳定非零退出码。
- FR-CLI-004：两个版本的 CLI 输出、`--json` 字段及错误码应保持一致。

## 6. 编码格式要求

### 6.1 格式原则

- 使用自描述、带版本号的容器格式，避免依赖 PNG 文件名或外部参数。
- 元数据和文件内容共同构成逻辑载荷，再按每像素 3 字节写入 RGB 通道。
- Alpha 通道如存在必须固定为 `255`，解码只读取 RGB。
- PNG 必须以无损方式读写，禁止缩放、裁剪、调色或重新压缩为有损图片。

### 6.2 v1 逻辑载荷

正式实现前以测试向量冻结具体二进制布局。建议的 v1 顺序如下：

1. 固定魔数，例如 ASCII `FTRN`。
2. 格式主版本和次版本。
3. 头部长度。
4. 原文件长度，使用无符号 64 位整数。
5. UTF-8 文件名长度与安全的原始基础文件名。
6. 32 字节 SHA-256。
7. 可选字段标志及保留区。
8. 原文件字节。
9. 为填满最后一个像素而添加的零字节。

### 6.3 容量限制

- 格式层面使用 64 位长度，不沿用参考实现的 24 位限制。
- 产品层面的默认最大文件大小由配置项控制；MVP 建议为 100 MiB，需在性能测试后最终确认。
- 编码前必须检查理论 PNG 尺寸、内存需求，以及所选 PNG 库的安全限制。
- 超出限制时应在读取或转换前尽早拒绝，并提示 CLI 配置方式。

### 6.4 兼容策略

- v1 解码器必须拒绝未知主版本，不得猜测解析。
- 次版本新增可选字段时应保持向后兼容。
- 是否兼容参考 TypeScript 版 `0xAF` 格式属于可选迁移需求，不纳入 MVP；如后续加入，应作为只读的 legacy 解码器并通过显式检测启用。
- `FORMAT.md` 与固定测试向量是格式行为的共同权威；两个实现均不得偏离规范。
- Python 版生成的图片必须可被 Node.js 版解码，Node.js 版生成的图片也必须可被 Python 版解码。

## 7. 技术方案与职责边界

### 7.1 Python 版

- 使用 Python 标准库实现 PNG 读写。
- 使用 Python 标准库提供仅限本机的服务和同款浏览器界面。
- 独立实现格式封装、编码、解码、检查、哈希校验和 CLI，不调用 Node.js。
- 优先使用流式或分块处理，避免不必要的多份完整文件拷贝。
- 提供单元测试、端到端往返测试和固定测试向量。

### 7.2 Node.js 版

- 使用 Node.js 标准库实现 PNG 读写。
- 提供仅监听 `127.0.0.1` 的本地 HTTP 服务和同款浏览器界面。
- 独立实现格式封装、编码、解码、检查、哈希校验和 CLI，不调用 Python。
- 管理临时目录、任务生命周期、端口选择、浏览器打开和退出清理。
- 使用轻量实现，不引入 Electron。

### 7.3 共享契约

- 两个版本共享 `FORMAT.md`、固定测试向量、API/CLI 行为约定和尽可能相同的前端静态资源。
- 两个版本不共享运行时代码，不通过子进程互相调用。
- 格式变更必须先更新规范和测试向量，再同步更新两个版本。

### 7.4 启动器

- 启动器负责定位本版本目录、检查单一运行时版本并启动本地服务。
- 两个版本均不依赖第三方包，因此首次启动不需要安装依赖。
- 所有路径必须支持空格、中文和非 ASCII 字符。
- 不制作 `.exe`、`.app`、Electron 包或其他独立安装包。

## 8. 非功能需求

### 8.1 安全与隐私

- NFR-SEC-001：服务只监听 `127.0.0.1`，不得默认监听 `0.0.0.0`。
- NFR-SEC-002：不进行文件上传、遥测或远程日志上报。
- NFR-SEC-003：上传名和元数据文件名必须清理，临时目录由应用创建且不可由输入路径控制。
- NFR-SEC-004：防止任意路径读写、命令注入、符号链接绕过和超大输入导致的资源耗尽。
- NFR-SEC-005：下载结果使用短生命周期随机标识，不能接受任意文件路径作为下载参数。

### 8.2 性能与稳定性

- NFR-PERF-001：处理时显示活动状态，页面不得表现为无响应。
- NFR-PERF-002：50 MiB 文件的往返转换应在目标机器上稳定完成，且结果哈希一致。
- NFR-PERF-003：单任务失败不得导致本地服务退出。
- NFR-PERF-004：异常退出后，下次启动应能清理本项目遗留的临时文件。

### 8.3 兼容性

- NFR-COMP-001：支持 Windows 10/11 x64。
- NFR-COMP-002：支持当前两个主版本范围内的 macOS，覆盖 Apple Silicon；Intel 支持情况在测试后确认。
- NFR-COMP-003：Python 版建议基线为 Python 3.11+，Node.js 版建议基线为 Node.js 20 LTS+；用户只需满足所选版本的基线。
- NFR-COMP-004：支持包含空格、中文和常见 Unicode 字符的路径及文件名。

### 8.4 可维护性

- NFR-MAIN-001：两个版本分目录组织，共享规范和测试夹具，业务实现彼此独立。
- NFR-MAIN-002：依赖版本应锁定，安装步骤可重复执行。
- NFR-MAIN-003：关键格式规则必须有固定测试向量，避免语言或版本升级引入静默不兼容。
- NFR-MAIN-004：同一测试语料在两个版本运行，并增加双向跨版本互操作测试。

## 9. 建议目录结构

```text
file-transfer/
├── README.md
├── REQUIREMENTS.md
├── PLAN.md
├── FORMAT.md
├── shared/
│   ├── index.html
│   ├── app.js
│   ├── styles.css
│   └── fixtures/
├── python-version/
│   ├── requirements.txt
│   ├── start.command
│   ├── start.bat
│   ├── file_transfer/
│   │   ├── __main__.py
│   │   ├── codec.py
│   │   ├── format.py
│   │   ├── server.py
│   │   └── errors.py
│   └── tests/
└── node-version/
    ├── package.json
    ├── package-lock.json
    ├── start.command
    ├── start.bat
    ├── src/
    │   ├── cli.js
    │   ├── codec.js
    │   ├── format.js
    │   └── server.js
    └── tests/
```

目录名称可在实现阶段微调，但两个版本必须能够被单独复制、安装和运行。

## 10. MVP 范围

### 10.1 包含

- 单文件编码与解码。
- v1 自描述 PNG 格式。
- SHA-256 校验和原文件名恢复。
- Python 版完整的 codec、CLI、本地服务和浏览器 UI。
- Node.js 版完整的 codec、CLI、本地服务和浏览器 UI。
- 两个版本各自的 macOS `.command` 与 Windows `.bat` 双击启动脚本。
- 核心单元测试、同版本往返测试、跨版本互操作测试及跨平台人工验收清单。

### 10.2 暂不包含

- `.exe`、`.app`、Electron 或其他原生桌面安装包。
- 多文件直接打包；用户可先自行压缩为 ZIP。
- 加密、密码保护、压缩算法。
- 图片美化、封面叠加、水印或容错编码。
- 云端存储、账户系统和远程传输。
- 对参考旧格式的兼容解码。
- 移动端适配。

## 11. 验收标准

- AC-001：空文件、1/2/3/4 字节文件、文本、图片、ZIP 和 50 MiB 随机文件均可往返恢复，哈希一致。
- AC-002：原文件名含空格、中文、emoji 和多点扩展名时可安全恢复。
- AC-003：修改编码 PNG 的任一有效载荷像素后，解码结果校验失败并显示错误。
- AC-004：普通 PNG 被拒绝，并提示“不是 File Transfer 生成的图片”。
- AC-005：未知主版本被拒绝，并显示版本不兼容。
- AC-006：macOS Finder 分别双击两个版本的 `start.command`，均可自动打开页面并完成一次往返转换。
- AC-007：Windows 资源管理器分别双击两个版本的 `start.bat`，均可自动打开页面并完成一次往返转换。
- AC-008：项目路径和输入路径包含空格及中文时，启动与转换均成功。
- AC-009：服务只能从本机访问；退出后本次任务临时文件被清理。
- AC-010：缺少所选版本运行时或依赖时，其启动器给出具体安装/初始化提示，不直接闪退，也不要求安装另一种运行时。
- AC-011：Python 编码结果可由 Node.js 解码且哈希一致；Node.js 编码结果可由 Python 解码且哈希一致。
- AC-012：同一输入在两个版本中展示一致的元数据、预计尺寸、错误类别和操作流程。

## 12. 风险与待确认项

- PNG 会比原文件产生额外体积，且随机数据通常无法被 PNG 高效压缩，需要在 UI 中提前说明。
- 构建整张图片会产生较高内存占用，100 MiB 默认上限需通过基准测试继续验证。
- 部分聊天或图片平台会缩放、转码或清除原始 PNG 数据，经过这些平台后文件可能无法恢复；本工具不能承诺跨平台传图链路保持有效。
- macOS 首次双击下载得到的脚本可能受执行权限或 Gatekeeper 影响，需要在文档和测试中给出处理方式。
- 纯源码分发仍要求用户预装 Python 或 Node.js 中的一种；本项目明确不提供零运行时安装包。
- 两套独立实现存在行为漂移风险，必须以共同格式规范、固定测试向量和跨版本测试控制。
- 实施前需最终确认：默认最大文件大小、macOS Intel 支持、是否加入 legacy `0xAF` 格式只读兼容。
